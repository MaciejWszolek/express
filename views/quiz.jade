extends layout 

block content 
  h1= title 
  if(show)
    form(method='post')
      p Jaka jest Twoja ulubona pora rok?
      ul 
        each item in data   
          li 
            label=item.title
            input(type='radio', name='quiz' value=item._id)
      input(type="submit" value="zagłosuj")
      p Aby zobaczyć wyniki oddaj głos
  else 
    h1 Dokonałeś już głosu w ankiecie 
    p Wyniki 
    ul 
    each item in data 
      li #{item.title} głosów: #{item.vote} (#{(item.vote/sum*100).toFixed()}%)

  p Głosów  oddano: #{sum}